# 🔧 Устранение проблем с взаимодействием

## ✅ Проблема: Взаимодействие не работает

### Быстрая проверка (5 шагов)

1. **Захватить курсор**
   - Кликните **ЛКМ** в окне Game
   - Курсор должен исчезнуть
   - Если нет - нажмите `ESC` и снова кликните

2. **Проверить консоль (Console)**
   - Откройте: `Window → General → Console`
   - Должны появляться сообщения: "Навели на: ..."
   - Если нет - читайте дальше

3. **Проверить красный луч**
   - В Scene view (режим Play)
   - Должен быть виден **красный/зелёный луч** от камеры
   - Зелёный = попадает на объект
   - Красный = ничего не видит

4. **Подойти ближе**
   - Максимальная дистанция: **5 метров**
   - Подойдите вплотную к объекту

5. **Пересоздать сцену**
   - `Ферма → Очистить сцену`
   - `Ферма → Создать простую ферму`

---

## 🔍 Подробная диагностика

### 1. Проверка Player Controller

**Что проверить:**

1. В Hierarchy найдите объект **"Player"**
2. В Inspector проверьте компонент **NonVRPlayerController**:
   - ✅ `Player Camera` должна быть заполнена (PlayerCamera)
   - ✅ `Interaction Range` = **5** (было 3)
   - ✅ `Interactable Layer` = **Everything** (или Default)
   - ✅ `Show Debug` = **✓** (включено)

**Если Show Debug включен:**
- В Console должны появляться сообщения при наведении
- "Навели на: PlantBed_1 (расстояние: 2.5м)"
- "Взаимодействие с: PlantBed_1"

### 2. Проверка коллайдеров

**Грядка (PlantBed):**
1. Выберите **PlantBed_1** в Hierarchy
2. В Inspector должен быть **Box Collider**:
   - ✅ Галочка включена
   - ✅ `Is Trigger` = **НЕТ** (выключено)
   - ✅ `Size` примерно (2, 0.5, 1)

**Лейка (WateringCan):**
1. Выберите **WateringCan** в Hierarchy
2. В Inspector должен быть **Box Collider**:
   - ✅ Галочка включена
   - ✅ `Is Trigger` = **НЕТ**
   - ✅ `Size` примерно (0.5, 0.6, 0.7)

**Бочка (WaterBarrel):**
1. Выберите **WaterBarrel** в Hierarchy
2. В Inspector должен быть **Box Collider**:
   - ✅ Галочка включена
   - ✅ `Is Trigger` = **ДА** (включено!)
   - ✅ `Size` примерно (1.5, 2, 1.5)
3. **Tag** должен быть **"Water"**

**Курица (Chicken):**
1. Выберите **Chicken_1** в Hierarchy
2. В Inspector должен быть **Capsule Collider**:
   - ✅ Галочка включена
   - ✅ `Is Trigger` = **НЕТ**

### 3. Проверка камеры

1. Нажмите **Play**
2. Выберите **Player → PlayerCamera**
3. В Scene view должен быть виден **красный луч** от камеры вперед
4. Если луча нет - `Show Debug` выключен или камера не найдена

### 4. Проверка расстояния

В режиме Play:
1. Откройте **Console** (`Ctrl+Shift+C`)
2. Наведите на объект
3. Должно появиться: **"Навели на: PlantBed_1 (расстояние: 2.5м)"**
4. Если расстояние > 5м - подойдите ближе

### 5. Проверка Input System

1. `Edit → Project Settings → Player`
2. `Active Input Handling` должен быть:
   - **"Input Manager (Old)"** ✅
   - или **"Both"** ✅
   - НЕ "Input System Package"

---

## 🛠️ Решения типичных проблем

### Проблема: "Навели на: ..." не появляется в Console

**Причина:** Show Debug выключен

**Решение:**
1. Выберите **Player** в Hierarchy
2. В компоненте **NonVRPlayerController**
3. Поставьте галочку **Show Debug** ✓

---

### Проблема: Луч красный, не попадает на объекты

**Причина:** Нет коллайдеров или они выключены

**Решение:**
1. Выберите объект (PlantBed, WateringCan и т.д.)
2. Проверьте наличие **Collider** компонента
3. Убедитесь, что галочка включена
4. Если коллайдера нет - добавьте:
   - `Add Component → Physics → Box Collider`

---

### Проблема: Луч зелёный, но взаимодействие не работает

**Причина:** Курсор не захвачен или нет компонента скрипта

**Решение 1 (курсор):**
- Кликните **ЛКМ** в окне Game
- Должно появиться: "Взаимодействие с: ..."

**Решение 2 (скрипты):**
1. Проверьте наличие скриптов на объектах:
   - PlantBed → компонент **PlantBed**
   - WateringCan → компонент **WateringCan**
   - Chicken → компонент **Animal**
   - WaterBarrel → компонент **WaterSource**

---

### Проблема: "Объект не является интерактивным"

**Причина:** Нет нужного компонента или тега

**Решение:**
1. В Console смотрите: "Компоненты: ..."
2. Должны быть:
   - PlantBed на грядке
   - Animal на курице
   - WateringCan на лейке
   - Tag "Water" на бочке

---

### Проблема: Грядка не поливается

**Причина:** Лейка не взята или пустая

**Решение:**
1. Сначала возьмите лейку (подойдите, нажмите E)
2. Наполните у бочки (подойдите к бочке, нажмите E)
3. Теперь поливайте грядку

**Проверка в Console:**
- "Берем лейку" ✓
- "Наполняем лейку" ✓
- "Поливаем грядку" ✓

---

### Проблема: Нельзя посадить растение

**Причина:** Грядка не полита или нет префаба растения

**Решение 1 (полив):**
- Убедитесь, что грядка полита (темная)

**Решение 2 (префаб):**
1. Выберите **PlantBed_1** в Hierarchy
2. В компоненте **PlantBed**
3. Проверьте поле **Plant Prefab**
4. Если пусто - создайте простой префаб:
   - `GameObject → Create Empty`
   - Назовите "Plant"
   - Добавьте компонент **Plant**
   - Перетащите в Prefabs
   - Присвойте в PlantBed

---

## 📊 Чек-лист для полной диагностики

### Перед запуском игры

- [ ] Сцена создана через `Ферма → Создать простую ферму`
- [ ] В Hierarchy есть:
  - [ ] Player (с NonVRPlayerController)
  - [ ] GameManager (с SimpleGameManager)
  - [ ] Ground (земля)
  - [ ] PlantBed_1, PlantBed_2, PlantBed_3, PlantBed_4
  - [ ] WateringCan
  - [ ] WaterBarrel
  - [ ] Chicken_1, Chicken_2

### В режиме Play

- [ ] Курсор захвачен (исчез после клика)
- [ ] WASD двигает игрока
- [ ] Мышь поворачивает камеру
- [ ] В Scene view виден луч от камеры
- [ ] В Console появляются сообщения "Навели на: ..."
- [ ] При нажатии E появляется "Взаимодействие с: ..."

### Тест взаимодействия

- [ ] Подошёл к лейке, нажал E → "Берем лейку"
- [ ] Подошёл к бочке, нажал E → "Наполняем лейку"
- [ ] Подошёл к грядке, нажал E → "Поливаем грядку"
- [ ] Подошёл к грядке, нажал E → "Сажаем растение"
- [ ] Подошёл к курице, нажал E → "Гладим животное"

---

## 🚀 Быстрое исправление

### Вариант 1: Пересоздать всё

```
1. Ферма → Очистить сцену
2. Ферма → Создать простую ферму
3. Play
4. Кликнуть ЛКМ
5. Играть!
```

### Вариант 2: Только Player

1. Удалите Player из Hierarchy
2. `Ферма → Создать простую ферму` (создаст только Player)
3. Play

---

## 📞 Всё ещё не работает?

1. Откройте **Console** (`Ctrl+Shift+C`)
2. Очистите (`Clear`)
3. Нажмите **Play**
4. Двигайте мышью на объект
5. Скопируйте все сообщения из Console
6. Проверьте ошибки (красные сообщения)

---

## 💡 Полезные команды

### В Console

- `Ctrl+Shift+C` - открыть Console
- `Clear` - очистить Console
- Двойной клик на сообщение - перейти к источнику

### В Scene view (во время Play)

- `F` на выбранном объекте - приблизить камеру
- Выберите Player → увидите луч взаимодействия

---

**Удачи! 🌾**
